# NewsAgent - Telegram бот для новостей

Простой сервер для работы с Telegram ботом.

## Быстрый старт

### 1. Установите uv (если еще не установлен)
```bash
# Mac/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# Или через pip
pip install uv
```

### 2. Установите зависимости
```bash
uv sync
```

### 3. Настройте переменные окружения
Создайте файл `.env` в корне проекта:
```bash
cp .env.example .env
```

Затем откройте `.env` и укажите:
- `TELEGRAM_BOT_TOKEN` - получите у @BotFather
- `TELEGRAM_CHAT_ID` - получите у @userinfobot

**Важно:** Файл `.env` уже добавлен в `.gitignore` и не попадет в Git!

### 4. Запустите простой бот для проверки
```bash
uv run python simple_bot.py
```

### 5. Проверьте работу
Найдите вашего бота в Telegram и отправьте `/start`

## Запуск в Docker 🐳

### Быстрый старт с Docker

1. Убедитесь, что у вас установлен Docker и Docker Compose

2. Создайте файл `.env` (если еще не создан):
```bash
cp .env.example .env
# Отредактируйте .env и укажите ваши токены
```

3. Соберите и запустите контейнер:
```bash
# С помощью docker-compose (рекомендуется)
docker-compose up -d

# Или с помощью docker напрямую
docker build -t newsagent .
docker run -d --name newsagent-bot --env-file .env newsagent
```

4. Проверьте логи:
```bash
docker-compose logs -f
# или
docker logs -f newsagent-bot
```

5. Остановка:
```bash
docker-compose down
# или
docker stop newsagent-bot && docker rm newsagent-bot
```

### Преимущества Docker

- ✅ Изолированное окружение
- ✅ Легкий деплой на любой сервер
- ✅ Автоматический перезапуск при сбоях
- ✅ Не нужно устанавливать Python и зависимости на хост-системе

📖 **Подробная инструкция:** Смотрите файл `DOCKER.md` для детальной информации по работе с Docker.

## Безопасность 🔒

**Важно:** Токены и секреты хранятся в файле `.env`, который:
- ✅ Добавлен в `.gitignore` и не попадет в Git
- ✅ Не должен быть закоммичен в репозиторий
- ✅ Используйте `.env.example` как шаблон (без реальных токенов)

Если вы случайно закоммитили `.env` с токенами:
1. Немедленно отзовите токен у @BotFather
2. Создайте новый токен
3. Удалите `.env` из истории Git (используйте `git filter-branch` или BFG Repo-Cleaner)

## Подробная инструкция

Смотрите файл `SETUP.md` для пошаговой инструкции по настройке.

