[project]
name = "newsagent"
version = "0.1.0"
description = "Telegram бот для сбора и отправки новостей"
requires-python = ">=3.8"
dependencies = [
    "python-telegram-bot>=20.0",
    "python-dotenv>=1.0.0",
]

