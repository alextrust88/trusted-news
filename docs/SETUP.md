# Пошаговая инструкция по настройке Telegram бота

## Шаг 1: Создание бота в Telegram

**⚠️ Важно:** Каждый разработчик должен создать **своего собственного бота** для тестирования, чтобы избежать конфликтов при одновременной работе.

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Введите имя для вашего бота (например: "Мой новостной бот (тест)")
4. Введите username для бота (должен заканчиваться на `bot`, например: `my_news_bot_test`)
5. **Скопируйте токен**, который выдаст BotFather (выглядит примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Почему нужен отдельный бот?**
- Telegram не позволяет нескольким экземплярам одного бота получать обновления одновременно
- Если несколько разработчиков используют один токен, возникает ошибка "Conflict: terminated by other getUpdates request"
- Каждый разработчик должен иметь свой токен для локальной разработки

## Шаг 2: Получение Chat ID

1. Найдите бота **@userinfobot** в Telegram
2. Отправьте ему любое сообщение
3. Бот ответит с вашим Chat ID (число, например: `123456789`)
4. **Скопируйте этот Chat ID**

## Шаг 3: Установка uv (если еще не установлен)

```bash
# Mac/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# Или через pip
pip install uv

# Проверьте установку
uv --version
```

## Шаг 4: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
# Перейдите в директорию бота
cd src/bot

# uv автоматически создаст виртуальное окружение и установит зависимости
uv sync
```

Это создаст виртуальное окружение `.venv` в директории `src/bot/` и установит все необходимые пакеты.

## Шаг 5: Настройка переменных окружения

**Важно:** Бот читает переменные только из окружения. В Docker переменные передаются через `env_file` в `docker-compose.yml`.

### Для Docker (рекомендуется)

1. Создайте файл `.env` в корне проекта (используется Docker Compose):
```bash
# Создайте файл .env на основе примера
cp .env.example .env

# Отредактируйте .env и замените значения
nano .env  # или используйте любой редактор
```

2. Docker Compose автоматически передаст переменные из `.env` в контейнер

### Для локальной разработки (без Docker)

Экспортируйте переменные окружения:

```bash
export TELEGRAM_BOT_TOKEN=ваш_токен_здесь
export TELEGRAM_CHAT_ID=ваш_chat_id_здесь
```

Или добавьте в `~/.bashrc` или `~/.zshrc`:

```bash
echo 'export TELEGRAM_BOT_TOKEN=ваш_токен' >> ~/.zshrc
echo 'export TELEGRAM_CHAT_ID=ваш_chat_id' >> ~/.zshrc
source ~/.zshrc
```

Пример содержимого `.env`:
```
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

**Безопасность:**
- Файл `.env` уже добавлен в `.gitignore` и не будет закоммичен в Git
- Никогда не публикуйте файл `.env` в репозиторий
- Для примера используйте файл `.env.example` (без реальных токенов)

## Шаг 6: Запуск простого бота для проверки

```bash
cd src/bot
uv run python simple_bot.py
```

Или если хотите активировать виртуальное окружение вручную:
```bash
source .venv/bin/activate  # Mac/Linux
# или
.venv\Scripts\activate  # Windows

python simple_bot.py
```

Если все настроено правильно, вы увидите:
```
🤖 Запуск Telegram бота...
   Нажмите Ctrl+C для остановки
```

## Шаг 7: Проверка работы

1. Найдите вашего бота в Telegram (по username, который вы указали)
2. Отправьте команду `/start`
3. Бот должен ответить и показать ваш Chat ID

Если бот отвечает - всё работает! ✅

## Возможные проблемы

### Ошибка: "Invalid token" или "TELEGRAM_BOT_TOKEN не установлен"
- Проверьте, что файл `.env` существует в корне проекта
- Проверьте, что токен скопирован полностью и без лишних пробелов в `.env`
- Убедитесь, что в `.env` нет кавычек вокруг значений (просто: `TELEGRAM_BOT_TOKEN=ваш_токен`)
- Проверьте, что файл `.env` находится в той же папке, что и `config.py`

### Бот не отвечает
- Убедитесь, что сервер запущен (cd src/bot
uv run python simple_bot.py)
- Проверьте интернет-соединение
- Убедитесь, что вы написали боту первым (некоторые боты требуют этого)

### Ошибка при установке зависимостей
- Убедитесь, что используете Python 3.8 или выше: `python3 --version`
- Убедитесь, что uv установлен: `uv --version`
- Попробуйте обновить uv: `pip install --upgrade uv` или переустановите через официальный скрипт

## Следующие шаги

После успешной проверки простого бота, можно переходить к созданию полноценного сервера для сбора новостей.

